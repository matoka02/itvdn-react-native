import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  mainContainer: {
    flex: 1,
  },
  title: {
    display: 'flex',
    alignSelf: 'center',
    fontSize: 24,
    fontWeight: 'bold',
    marginTop: 30,
    marginBottom: 80,
  },
});

export default styles;
